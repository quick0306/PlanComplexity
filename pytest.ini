[pytest]
# Windows Tcl/Tk can retain standard-channel handles between GUI instances.
# fd capture replaces those OS handles and can cause intermittent Tcl file-read
# errors. Capture Python streams while leaving OS handles stable for real GUI tests.
addopts = --capture=sys
